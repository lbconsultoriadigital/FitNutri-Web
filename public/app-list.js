function updateMetrics(jobs){$('metricTotal').textContent=jobs.length;$('metricRunning').textContent=jobs.filter(j=>['queued','running'].includes(j.status)&&j.current_stage<6).length;$('metricReview').textContent=jobs.filter(j=>j.status==='review_required').length;$('metricApproved').textContent=jobs.filter(j=>j.status==='approved').length;$('jobCount').textContent=jobs.length}
async function loadJobs(showError=true){try{const jobs=await api('/api/atendimentos');state.jobs=jobs;updateMetrics(jobs);const root=$('jobs');root.replaceChildren();if(!jobs.length){root.append(text('div','Nenhum atendimento criado.','empty'));return}jobs.forEach(job=>{const card=document.createElement('article');card.className='job'+(job.id===state.selectedId?' selected':'');card.tabIndex=0;const top=document.createElement('div');top.className='job-top';top.append(text('div',job.patient_name,'job-name'));const badge=document.createElement('span');setBadge(badge,job.status);top.append(badge);card.append(top);const meta=[`Etapa ${job.current_stage}/6`,dateText(job.created_at)];if(job.exam_file_count)meta.push(`${job.exam_file_count} PDF${job.exam_file_count===1?'':'s'}`);card.append(text('div',meta.join(' · '),'job-meta'));const progress=document.createElement('div');progress.className='mini-progress';const bar=document.createElement('span');bar.style.width=`${Math.round(job.current_stage/6*100)}%`;progress.append(bar);card.append(progress);card.onclick=()=>loadDetail(job.id);card.onkeydown=e=>{if(e.key==='Enter')loadDetail(job.id)};root.append(card)})}catch(err){if(showError)notify(err.message)}}

async function loadDetail(id,showError=true){try{state.selectedId=id;const job=await api(`/api/atendimentos/${encodeURIComponent(id)}`);state.currentJob=job;renderDetail(job);return job}catch(err){if(showError)notify(err.message);return null}}
function renderDetail(job){$('detailEmpty').classList.add('hidden');$('detailContent').classList.remove('hidden');$('detailName').textContent=job.patient_name;$('detailMeta').textContent=`${job.slug} · criado em ${dateText(job.created_at)}`;setBadge($('detailStatus'),job.status);$('detailProgress').style.width=`${Math.round(job.current_stage/6*100)}%`;$('progressText').textContent=`Etapa ${job.current_stage} de 6`;$('processingMode').textContent=job.processing_mode==='manual'?'Execução controlada pelo painel':'Fila automática QStash';renderAgents(job);renderOverview(job);renderOutputs(job);renderReport(job);renderPdf(job);const complete=job.current_stage>=6||['review_required','approved'].includes(job.status);$('runNext').disabled=complete||state.processing;$('runAll').disabled=complete||state.processing;$('openPdfTop').classList.toggle('hidden',!job.has_exam_pdf);$('openPdfTop').textContent=job.exam_file_count>1?`Ver ${job.exam_file_count} PDFs`:'Abrir PDF';$('openPdfTop').onclick=()=>selectTab('pdf');loadJobs(false)}
function renderAgents(job){const root=$('agentGrid');root.replaceChildren();STAGES.forEach((stage,index)=>{const number=index+1;const card=document.createElement('div');card.className='agent'+(number<=job.current_stage?' done':number===job.current_stage+1&&['queued','running'].includes(job.status)?' active':'');card.append(text('div',number<=job.current_stage?'✓':String(number),'agent-index'));card.append(text('div',stage.label,'agent-label'));card.title=stage.detail;root.append(card)})}
